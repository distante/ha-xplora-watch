#! Test requirements for Xplora® Watch

# Constrain test dependencies to the committed runtime lock.
-c requirements-lock.txt

# Core test dependencies (pinned to match pytest-homeassistant-custom-component's own pins)
pytest==9.0.3
pytest-asyncio==1.4.0
pytest-cov==7.1.0

# Mocks aiohttp/GraphQL transport calls in xplora_watch tests without hitting the network.
# 0.7.9 (the latest release) still predates the aiohttp 3.14 ClientResponse signature HA 2026.7
# pulls in; tests/xplora_watch/conftest.py shims the missing `stream_writer` argument.
aioresponses==0.7.9

# Home Assistant testing dependencies
pytest-homeassistant-custom-component==0.13.345

# Required by HA's `ffmpeg` integration (an `after_dependency`): importing get_ffmpeg_manager in
# helper.py pulls in `haffmpeg`. HA installs this on demand in production, but the test env installs
# only from the locks, so it must be listed here or the suite errors at import. Pinned to the
# version HA's ffmpeg component requires.
ha-ffmpeg==3.2.2

# Static type checking
mypy==1.20.0
